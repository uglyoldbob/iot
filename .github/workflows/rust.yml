name: Rust

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Install system dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y pkgconf libpcsclite-dev softhsm2 libsofthsm2 help2man openjdk-8-jdk maven ant opensc
        cargo install wasm-pack
    - name: Setup jcardsim
      working-directory: ./jcardsim
      run: |
        mvn initialize
        mvn clean install
    - name: Setup virtualsmartcard
      working-directory: ./vsmartcard/virtualsmartcard
      run: |
        autoreconf -vis
        ./configure
        sudo make install
    - name: Setup pivapplet
      working-directory: ./
      run: |
        javac -classpath jcardsim/target/jcardsim-3.0.5-SNAPSHOT.jar PivApplet/src/net/cooperi/pivapplet/*.java
    - name: Build pivapplet
      working-directory: ./PivApplet
      run: |
        ant
    - uses: actions/checkout@v4
    - name: Build
      run: cargo build --verbose
    - name: Run tests
      run: cargo test --verbose
